[package]
name = "snag-tauri"
description = "Snag, the web build: the same logic behind an HTML interface."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
build = "build.rs"

[build-dependencies]
tauri-build = "2"

[dependencies]
snag-core.workspace = true
tauri = "2"
serde = { version = "1", features = ["derive"] }
rfd = "0.17"

# Build output. One target/ for the whole workspace.
target/

# Binaries are never committed: CI builds them and attaches them to a release.
*.exe
*.pdb

# Generated by tauri-build on every compile.
app-tauri/gen/schemas/

*.log
Thumbs.db
desktop.ini
.vscode/
.idea/
